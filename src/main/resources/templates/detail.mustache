<!DOCTYPE html>
<html lang="en" xmlns="http://www.w3.org/1999/html">
<head>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">

    <meta charset="UTF-8">
    <title>Detail</title>

    <style>
        html, body {
			margin: 0;
			padding:0;
			box-sizing:border-box;
			font-family: 'inter', sans-serif;
			background: #fff;
			line-height: 1;
			}


		/* 헤더 부분 */
        .header {
            padding: 10px 20px;
        }

		.exhibition-title {
			font-size: 15px;
			margin: 0;

			}

		.title-container {
			/*max-width: 1400px;*/
			margin: 0 auto;
			display: flex;
			align-items: baseline;
			justify-content: space-between;
            text-underline: black;
		}

		.exhibition-tag {
			font-size: 14px;
		}


		/*메인 콘텐츠*/
		.main-content{
			display: grid;
            grid-template-columns: 320px 1fr 400px;
			gap: 0;
			padding: 0;
            width: 100vw;
            height: 100vh;
            border-left: 2px solid #000;
            border-top: 2px solid #000;
            border-bottom: 2px solid #000;
            box-sizing: border-box;
        }

        .left-section {
            border-right: 2px solid #000;
            padding: 0 10px;
            overflow-y: auto;
            height: 100vh;
            box-sizing: border-box;
            background: #fafafa;

        }

        .right-section {
            border-right: 2px solid #000;
            padding: 10px;
            overflow-y: auto;
            height: 100vh;
            box-sizing: border-box;
        }


        /* 왼쪽 구역 */
        .left-section {
        	padding-right: 20px;
        }

		/* {description} */
        .exhibition-description {
            font-size: 40px;
            /*line-height: 1.8;*/
            color: #333;
            margin-bottom: 20px;
            text-align: justify;
			font-weight: bold;
        }

		.exhibition-info {
			font-size: 18px;
			margin-top: 20px;
		}

		.meta-info div {
			margin-bottom: 6px;
		}

		.meta-info strong {
			text-decoration: underline;
			text-underline-offset: 2px;
		}


		/*오르쪽 구역 - 이미지*/
		.image-section {
            padding: 10px;
            overflow-y: auto;
            height: 100vh;
            box-sizing: border-box;
		}

		.exhibition-image {
			width: 150%;
			height: 400px;
			object-fit: cover;
		}

        /* 통계 구역*/
		.down-section {
			padding: 20px 0;
			margin: 0 auto;
			max-width: 600px;
			margin-left: 40px;
		}

		.stat-item {
			display: flex;
			align-items: center;
			gap: 8px;
		}

		.stat-label {
			font-weight: bold;
		}

		.stats-container {
			max-width: 1200px;
			margin: 0 auto;
		}

        .left-section {
            background: #fafafa;
            padding: 10px;
        }

        .exhibition-item:hover {
            background-color: #f0f7ff;
        }




    </style>
</head>

<body  data-display-id="123">

<!--HEADER-->
<!--전시 제목 구역-->
<header>
<div class="header">
	<div class="title-container">
		<h1 class="exhibition-title">{{TITLE}}</h1>
		<span class="exhibition-tag" style="margin-left: auto">(Exhibition)</span>
	</div>
</div>
</header>


<div class="main-content">

    <!--왼쪽 / 전시 리스트 구역-->
    <aside class="left-section">
    <div class="exhibition-list">
        {{#allExhibitions}}
            <div class="exhibition-item">
                <span class="title-info" style="font-size: 15px; flex: 1">{{title}}</span>
                <span class="period-info" style="font-size: 15px; min-width: 120px; margin-right: 15px">{{period}}</span>
            </div>
        {{/allExhibitions}}
    </div>
    </aside>



	<!--중간 / 전시 설명 구역-->
<section class="right-section">
            <div class="exhibition-description">{{{DESCRIPTION}}}</div>
		<div class="exhibition-info">
			<div><strong>Author:</strong> {{AUTHOR}}</div>
			<div><strong>Institution:</strong> {{AGENCY}}</div>
			<div><strong>Period:</strong> {{PERIOD}}</div>
			<div><strong>congestion:</strong> {{congestionNm}}</div>
		</div>


    <!--중간 선-->
<!--    <div class="middle" style="	border-top: 1px solid #000000;-->
<!--			margin: 10px 20px; "></div>-->

    <!--@@@수정해야함@@@-->
    <!-- 다운 섹션: 통계 ???-->
    <section class="down-section">
        <h2 class="stats-title">Exhibition Analystics</h2>
        <div class="stats-grid">
            <div class="stat-item">
                <div class="stat-label">Congestion Score</div>
                <span class="stat-number" id="congestionScore">35</span>
            </div>

            <div class="stat-item">
                <div class="stat-label">Location Score</div>
                <span class="stat-number" id="locationScore">15</span>
            </div>

            <div class="stat-item">
                <div class="stat-label">Time Score</div>
                <span class="stat-number" id="timeScore">12</span>

            </div>

            <div class="stat-item">
                <div class="stat-label">Popularity Score</div>
                <span class="stat-number" id="popularityScore">8</span>
            </div>

            <!--추천 점슈 & 이유-->
            <div class="recommendation-details">
                <div class="recommendation-score" id="totalScore">85</div>
                <div class="recommendation-text" id="recommendationText">@@@ Highly recommended @@@</div>
            </div>
        </div>

    </section>
</section>


	<!--우측 이미지 구역-->
	<section class="image-section">
		        <div class="image-container">
		            <img src="{{imageObject}}" class="exhibition-image" >


        <!--버튼-->
		<div class="action-buttons">
			<a href="/main" class="btn"> View More Exhibitions</a>
		</div>

                </div>
        </section>

</div>
</body>



<script>


</script>
</html>